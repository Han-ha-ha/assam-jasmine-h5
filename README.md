# 统一阿萨姆茉莉奶绿 H5

纯前端互动小游戏当前版，包含五个原主页面、固定门票分享海报状态及全部基础交互流程。

## 本地预览

在当前目录启动任意静态文件服务器，例如：

```powershell
python -m http.server 8080
```

然后访问 `http://localhost:8080/`。

线上地址与日后自行修改概率、重新发布的步骤见 [上线与概率修改教程.md](./上线与概率修改教程.md)。

## 待替换配置

- `app.js` 中的 `CONFIG.recipientEmail`：当前暂用 `2998458181@qq.com` 接收中奖信息，品牌方提供正式邮箱后再替换。
- 默认开场页使用 `素材3/广州站源文件/横构图.psd` 导出的正式16:9主KV；地址增加 `kv=vertical` 时切换为艺人合集9:16无虚影KV。两种首页共用同一套游戏、抽奖、兑奖和邮件修复逻辑。
- 当前主游戏页使用 `素材3/web` 中经裁边压缩的地面、8 个透明场景区域及真实奶绿瓶，按“底部花艺拱门入口—中央茉莉奶茶铺—顶部主舞台终点”的长竖版路线编排；茉莉识曲与瓶替奶茶铺相对，茉莉花园位于茉莉节拍和好心情应援营地对面的中间。场景支持上下左右拖动和双指缩放，原始 PNG 保留在 `素材3`。
- 微信滑动性能已优化：7 个静态场景区域与地面、阴影预合成为一张移动端安全尺寸场景图，仅保留瓶替奶茶铺前景和 5 个奶绿为独立图层；奶绿图按移动端实际显示尺寸缩放，拖动/缩放按动画帧合并更新，并取消游戏顶部栏的实时背景模糊。
- **临时测试状态：** `app.js` 中 `CONFIG.temporaryWinProbability` 当前为 `0.5`（50%）。正式上线前必须删除该临时项并恢复下方北京时间概率表。
- 正式概率表位于 `CONFIG.winProbabilitySchedule`：2026-09-05 12:00 至 2026-09-06 19:00 为 `1/400000`，2026-09-12 12:00 至 2026-09-13 19:00 为 `1/600000`，其余时间均为 0。结束时间点不包含在中奖窗口内。
- 本机可使用 `?testReset=1&forceWin=1` 清空记录并令首次抽奖必中；本轮手机测试期间，GitHub Pages 可使用 `?testReset=1` 清空当前手机的本地测试记录，但 `forceWin=1` 在线上域名仍不会生效。正式上线前需关闭线上重置入口。
- UI、按钮、进度条、规则/手机号/结果/邮件提示弹窗和程序音效均已完成；BGM接入 `素材3/遇见茉莉好心情_final_MMM(1).mp3`，需用户首次点击后播放。
- 抽奖页使用 8 个独立转盘格：一等奖仅保留 1 格并接入客户提供的 `素材3/门票图.png`；重复的未中奖提示分散且不相邻，程序会让转盘真实停到本次抽奖结果对应的格子。
- 加载页会预加载当前关键图片和字体；场景支持上下左右拖动、双指缩放、缩放按钮、防误触和收集反馈。
- 首页会根据本机进度显示“开始探索”“继续找奶绿”或“继续抽奖”。
- 分享入口新增固定门票海报页，使用 `assets/share/ticket-share-poster.jpg`；用户长按保存后点击“下一步：去分享”，页面显示右上角微信菜单引导，再由用户手动完成分享并领取本地每日1次奖励。微信菜单分享的是活动链接，完整海报需从相册选择。
- `scripts/build_visual_assets.py` 用于从客户PSD重新导出首页KV并生成固定分享海报，二维码默认编码当前GitHub Pages活动地址。
- 页面会读取 `version.json` 检查版本；发现线上版本变化时提示用户刷新。
- 2.5D页面已接入 `2.5D/2.5D3.png` 转换得到的正式人物场景 WebP，并按客户标注重新设置5个点击区域；抽奖继续保留大转盘，不改为老虎机。
- 抽奖页直接使用门票分享海报同源的PSD“背景”图层，保持相同的蓝天、白云、草地视觉；转盘格子仍保持清晰可读。

## 当前字体

- 标题：客户提供的方正粗倩简体，页面加载 `字体文件/FZCuQian-H5.woff2` 子集。
- 正文：思源黑体，页面加载 `字体文件/SourceHanSansCN-H5.woff2` 子集，完整字体及授权文件同目录保存。
- 装饰文字：客户提供的胡晓波浪漫宋，页面加载 `字体文件/HXBLangManSong-H5.woff2` 子集。

## 基础版限制

- 使用 `localStorage` 保存本机状态及已中奖手机号；清缓存或更换设备可绕过限制，不能替代服务端防重复中奖。
- 抽奖为前端随机，无法精确控制库存或可靠防作弊。
- 分享奖励为用户手动确认，无法验证是否真实分享。
- 中奖信息通过 `mailto:` 尝试唤起邮件应用；微信内使用纯收件邮箱避免显示编码长串，并自动尝试复制完整中奖信息供用户粘贴，最终仍需用户在邮件应用中确认发送。
- 已接入当前测试用百度统计代码，仅采集百度统计默认的访问流量数据，不上报中奖事件，也不上传姓名、手机号或身份证。正式上线前需替换为客户百度统计账号生成的统计代码。

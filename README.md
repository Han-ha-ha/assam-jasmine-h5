# 统一阿萨姆茉莉奶绿 H5

纯前端互动小游戏占位版，包含五个主页面及全部基础交互流程。

## 本地预览

在当前目录启动任意静态文件服务器，例如：

```powershell
python -m http.server 8080
```

然后访问 `http://localhost:8080/`。

线上地址与日后自行修改概率、重新发布的步骤见 [上线与概率修改教程.md](./上线与概率修改教程.md)。

## 待替换配置

- `app.js` 中的 `CONFIG.recipientEmail`：当前暂用 `2998458181@qq.com` 接收中奖信息，品牌方提供正式邮箱后再替换。
- 当前主游戏页使用 `素材2/透明试验/08-茉莉奶茶铺-透明试验.png` 验证分层场景；功能与 UI 完善版已完整保存在 `版本归档/20260827-221205-功能与UI完善版`，后续按最近三版规则归档。
- `app.js` 中的 `CONFIG.winProbability`：正式发布中奖概率为 `0.02`（2%）。修改后需重新发布 GitHub Pages 才会对访客生效。
- UI、按钮、进度条、规则/分享/手机号/结果/邮件提示弹窗和程序音效均已完成，不依赖客户继续提供 UI 或音效素材。
- 加载页会预加载当前关键图片和字体；场景支持拖动、双指缩放、缩放按钮、防误触和收集反馈。
- 首页会根据本机进度显示“开始探索”“继续探索”或“继续抽奖”。
- 正式视觉素材到位后，替换页面背景、2.5D 场景、奶绿产品、转盘、弹窗和分享图。

## 当前字体

- 标题：客户提供的方正粗倩简体，页面加载 `字体文件/FZCuQian-H5.woff2` 子集。
- 正文：思源黑体，页面加载 `字体文件/SourceHanSansCN-H5.woff2` 子集，完整字体及授权文件同目录保存。
- 装饰文字：客户提供的胡晓波浪漫宋，页面加载 `字体文件/HXBLangManSong-H5.woff2` 子集。

## 基础版限制

- 使用 `localStorage` 保存本机状态及已中奖手机号；清缓存或更换设备可绕过限制，不能替代服务端防重复中奖。
- 抽奖为前端随机，无法精确控制库存或可靠防作弊。
- 分享奖励为用户手动确认，无法验证是否真实分享。
- `mailto:` 需要用户在邮件应用中手动确认发送。
- 百度统计位置已在 `index.html` 的 `<head>` 中预留，客户提供完整异步统计代码后再接入。

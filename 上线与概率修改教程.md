# H5 上线与中奖概率修改教程

## 当前线上地址

- 网站：<https://han-ha-ha.github.io/assam-jasmine-h5/>
- GitHub 仓库：<https://github.com/Han-ha-ha/assam-jasmine-h5>
- 发布方式：GitHub Pages，从 `main` 分支的根目录自动发布。

首次发布已经完成。今后只要把修改推送到 `main` 分支，GitHub Pages 会自动重新发布，通常等待 1–3 分钟即可。

## 当前正式中奖概率

- 2026-09-05 12:00 至 2026-09-06 19:00：`1/400000`。
- 2026-09-12 12:00 至 2026-09-13 19:00：`1/600000`。
- 其他全部时间：`0`。
- 所有时间均按北京时间（UTC+8）计算，结束时间点不包含在中奖窗口内。

## 正式入口与独立测试入口

- 正式入口：`https://han-ha-ha.github.io/assam-jasmine-h5/`
- 独立测试入口：`https://han-ha-ha.github.io/assam-jasmine-h5/?testMode=1&testReset=1`
- 测试入口固定为 50% 中奖概率，并使用 `assam-jasmine-h5-test-v1` 独立保存测试进度；每次进入只重置测试数据，不会清除同一手机上的正式进度。
- 测试入口不会加载客户百度统计代码，测试访问不计入正式统计；测试中奖邮件会带“测试数据”标记。
- 正式入口即使误带 `testReset=1`，也不会清除正式本地记录。

## 修改中奖概率（推荐用电脑操作）

1. 打开项目文件夹：`D:\part_time_job\3kH5`。
2. 用编辑器打开 `app.js`，在文件最上方找到 `winProbabilitySchedule`。每个时段包含开始时间、结束时间和概率，例如：

   ```js
   {
     startAt: "2026-09-05T12:00:00+08:00",
     endAt: "2026-09-06T19:00:00+08:00",
     probability: 1 / 400000,
   },
   ```

3. 修改对应时段或分母并保存。中奖窗口以外自动为 0；不要添加临时概率覆盖项。

4. 为避免访客继续使用旧缓存，同时打开 `index.html` 最底部，找到：

   ```html
   <script src="app.js?v=20260902-branded-status-icons-v35"></script>
   ```

   将 `v=` 后面的值改成一个新值即可，例如改为：

   ```html
   <script src="app.js?v=20260901-probability-update"></script>
   ```

5. 在该项目文件夹空白处右键，选择“在终端中打开”，依次执行：

   ```powershell
   git add app.js index.html README.md
   git commit -m "更新正式中奖概率"
   git push origin main
   ```

   第二条命令只是提交说明，可按实际修改内容调整。

6. 等待 1–3 分钟，访问线上地址。若仍显示旧效果，浏览器按 `Ctrl + F5` 强制刷新；手机可关闭网页后重新打开。

## 不使用终端，直接在 GitHub 网页修改

1. 登录 GitHub，打开仓库：<https://github.com/Han-ha-ha/assam-jasmine-h5>。
2. 点击 `app.js`，点击右上角铅笔图标，修改 `winProbabilitySchedule` 中的时段或概率。
3. 点击绿色 **Commit changes** 保存。
4. 返回仓库根目录，点击 `index.html`，同样用铅笔图标把 `app.js?v=` 后的版本文字改成一个新值，再 **Commit changes**。
5. 等待 GitHub Pages 自动发布；可在仓库顶部 **Actions** 查看进度，或直接刷新线上地址验证。

## 部署/重新部署检查

1. 确认代码已经推送至 `main` 分支。
2. 仓库中打开 **Settings → Pages**，应显示：
   - Source：`Deploy from a branch`
   - Branch：`main`
   - Folder：`/(root)`
3. 若页面不能访问，先等 3 分钟；再检查 **Actions** 或 Pages 页面是否显示发布错误。
4. 不要删除 `素材`、`素材2`、`字体文件` 中已被页面引用的文件，否则线上图片或字体会失效。

## 注意事项

- 正式运营使用上述北京时间概率表，代码中没有 50% 临时覆盖项。
- 50% 概率仅在地址明确带有 `testMode=1` 时启用；不要把独立测试地址作为正式活动二维码或对外正式入口。
- 这是纯前端随机概率，只能作为展示/基础运营功能，不能精确控制实际中奖库存或防止清缓存绕过。
- 中奖信息通过 `mailto:` 发往 `2214047289@qq.com`。
- 客户百度统计代码为 `f3af2f8610b5a7b5ff2c6c892b6d953a`，统计数据从该代码上线后开始进入客户账号，旧统计账号中的历史数据不会迁移。
